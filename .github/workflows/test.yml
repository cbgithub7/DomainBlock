name: Test

on:
  push:
  pull_request:

jobs:
  test:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run DomainBlock tests
        shell: pwsh
        run: ./tests/Invoke-DomainBlockTest.ps1
